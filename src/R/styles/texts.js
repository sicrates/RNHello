import { StyleSheet } from 'react-native'

const texts = StyleSheet.create({
    default: { fontSize: 12, color: '#000', },
    input: { fontSize: 12, color: '#000', paddingLeft: 20, paddingRight: 20, },
})

export default texts